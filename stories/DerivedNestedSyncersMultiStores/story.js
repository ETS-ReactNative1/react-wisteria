import React from 'react';
import App from '.';

export default { title: 'Nested relation chain with 4 syncers with 4 different stores' };

export const NestedRelationWithMultiStores = () => {
    return (<App/>);
};
